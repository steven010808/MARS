# Data Directory

Runtime data is intentionally excluded from Git.

Use the wiki Google Drive runtime bundle for prepared `data/processed` files, or place raw H&M and Microsoft H&M search files at the paths documented in `docs/runtime_bundle_guide.md`.
